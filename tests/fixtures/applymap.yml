# pandas applymap
in_uri: ./testdata.csv
copy: true
read:
  skipfooter: 1
columns:
  - value: amount
  - state
  - date
dt_index: date
ops:
  - applymap:
      func: 'lambda x: x.lower() if isinstance(x, str) else x'
